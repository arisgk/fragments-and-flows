---
import { Icon } from 'astro-icon/components';

interface Props {
  text: string;
  size?: 'sm' | 'md' | 'lg';
  icon?: string;
  classes?: string;
  type?: 'button' | 'reset' | 'submit' | null;
}

const { text, icon, size = 'md', classes, type = 'button' } = Astro.props;

const paddingClasses = {
  sm: 'gap-x-1.5 px-2.5 py-1.5',
  md: 'gap-x-1.5 px-3 py-2',
  lg: 'gap-x-2 px-3.5 py-2.5',
}[size];
---

<button
  type={type}
  class=`inline-flex items-center justify-center bg-indigo-500 text-white dark:text-black hover:dark:bg-indigo-300 dark:bg-indigo-200 text-sm font-semibold shadow-sm hover:bg-indigo-500/85 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-black cursor-pointer ${paddingClasses} ${classes ?? ''}`
>
  {icon && <Icon name={icon} aria-hidden="true" class="-ml-0.5 size-5" />}
  {text}
</button>
